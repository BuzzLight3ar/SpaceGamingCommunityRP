name: SpaceGamingCommunityRP
version: 1.0.0
author: BuzzLight3ar

tasks:

  # -------------------------
  # Folder Structure
  # -------------------------
  - action: create_dir
    path: resources/[core]

  - action: create_dir
    path: resources/[qb]

  - action: create_dir
    path: resources/[standalone]

  - action: create_dir
    path: resources/[eup]

  # -------------------------
  # Core Dependencies
  # -------------------------
  - action: download_github
    src: https://github.com/overextended/ox_lib
    dest: resources/[stand
